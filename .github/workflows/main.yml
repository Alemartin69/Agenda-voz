name: Build Android APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Instalar dependencias
        run: npm install --legacy-peer-deps
        working-directory: agenda-voz

      - run: npm install -g eas-cli

      - name: Generar keystore
        run: |
          keytool -genkeypair -v \
            -keystore ${{ github.workspace }}/agenda-voz/agenda-voz.keystore \
            -alias agenda-voz \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -storepass android123 -keypass android123 \
            -dname "CN=AgendaVoz,O=AgendaVoz,C=AR"

      - name: Crear credentials.json
        run: |
          cat > ${{ github.workspace }}/agenda-voz/credentials.json << 'EOF'
          {
            "android": {
              "keystore": {
                "keystorePath": "${{ github.workspace }}/agenda-voz/agenda-voz.keystore",
                "keystorePassword": "android123",
                "keyAlias": "agenda-voz",
                "keyPassword": "android123"
              }
            }
          }
          EOF

      - name: Registrar proyecto en Expo
        run: eas init --force --non-interactive
        working-directory: agenda-voz
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: Compilar APK
        run: eas build -p android --profile preview --non-interactive --local --output ${{ github.workspace }}/agenda-voz-release.apk
        working-directory: agenda-voz
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - uses: actions/upload-artifact@v4
        with:
          name: agenda-voz-apk
          path: agenda-voz-release.apk
          retention-days: 30
